# 7. LazyColumn acessível

```kotlin
LazyColumn(
    modifier = Modifier.semantics { contentDescription = "Lista de produtos" }
) {
    items(produtos) { produto ->
        Text(
            text = produto,
            modifier = Modifier.padding(16.dp)
        )
    }
}
```

Garanta espaçamento entre os itens e conteúdo acessível.